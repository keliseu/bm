
import 'package:flutter/material.dart';

class nycMAPAPP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Home Screen A')),
      body: Center(
        child: Text('Welcome to option A!'),
      ),
    );
  }
}