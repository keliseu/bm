

import 'package:flutter/material.dart';

class aucklandMAPAPP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Home Screen A')),
      body: Center(
        child: Text('Welcome to option A!'),
      ),
    );
  }
}